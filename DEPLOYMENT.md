# 🚀 Инструкция по развёртыванию MoraTalk Bot 24/7

## 📋 Содержание
1. [Render.com (РЕКОМЕНДУЮ - БЕСПЛАТНО)](#1-rendercom-бесплатно)
2. [Railway.app (Бесплатно + платные планы)](#2-railwayapp)
3. [PythonAnywhere (Бесплатно с ограничениями)](#3-pythonanywhere)
4. [VPS сервер (Полный контроль)](#4-vps-сервер)
5. [Heroku (Платно, но надёжно)](#5-heroku)

---

## 1. Render.com (БЕСПЛАТНО) ⭐ РЕКОМЕНДУЮ

### Преимущества:
✅ Полностью бесплатно
✅ Автоматические перезапуски
✅ Простая настройка
✅ GitHub интеграция

### Шаги:

#### Шаг 1: Подготовка
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте аккаунт на [GitHub](https://github.com) (если нет)

#### Шаг 2: Загрузка кода на GitHub
1. Создайте новый репозиторий на GitHub
2. Загрузите все файлы проекта:
   - `moratalk_bot.py`
   - `requirements.txt`
   - `Dockerfile`
   - `render.yaml`
   - `.gitignore`

```bash
# В командной строке:
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/moratalk-bot.git
git push -u origin main
```

#### Шаг 3: Развёртывание на Render
1. Войдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Background Worker"**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий `moratalk-bot`
5. Настройки:
   - **Name**: `moratalk-bot`
   - **Environment**: `Docker`
   - **Plan**: `Free`
6. В **Environment Variables** добавьте:
   - **Key**: `BOT_TOKEN`
   - **Value**: ваш токен от @BotFather
7. Нажмите **"Create Background Worker"**

#### Шаг 4: Готово! 🎉
Бот автоматически развернётся и начнёт работать 24/7!

**⚠️ Важно**: На бесплатном плане Render усыпляет сервис после 15 минут неактивности. Для настоящего 24/7 нужен платный план ($7/месяц).

---

## 2. Railway.app

### Преимущества:
✅ $5 бесплатно каждый месяц
✅ Очень простой деплой
✅ Автоматические обновления

### Шаги:

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий
4. Railway автоматически определит Python проект
5. Добавьте переменную окружения:
   - `BOT_TOKEN` = ваш токен
6. Нажмите **"Deploy"**

**Стоимость**: $5 бесплатно/месяц, потом ~$5-10/месяц

---

## 3. PythonAnywhere (БЕСПЛАТНО с ограничениями)

### Преимущества:
✅ Бесплатный план
✅ Не нужен Docker
✅ Простой интерфейс

### Недостатки:
❌ Нужно перезапускать каждые 3 месяца
❌ Ограниченный CPU

### Шаги:

1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Перейдите в **"Consoles"** → **"Bash"**
3. Выполните команды:

```bash
# Клонируем репозиторий
git clone https://github.com/YOUR_USERNAME/moratalk-bot.git
cd moratalk-bot

# Устанавливаем зависимости
pip3 install --user -r requirements.txt

# Создаём переменную окружения
echo "export BOT_TOKEN='ваш_токен'" >> ~/.bashrc
source ~/.bashrc

# Запускаем бота
python3 moratalk_bot.py
```

4. Чтобы бот работал постоянно, создайте **Always-on task** (требуется платный аккаунт $5/мес)

**Бесплатный вариант**: Запускайте бота вручную после каждого перезапуска системы.

---

## 4. VPS Сервер (Полный контроль)

### Рекомендуемые провайдеры:
- **DigitalOcean** - от $4/месяц
- **Vultr** - от $2.50/месяц  
- **Linode** - от $5/месяц
- **Timeweb (RU)** - от 190₽/месяц

### Шаги (для Ubuntu):

#### Шаг 1: Подключение к серверу
```bash
ssh root@YOUR_SERVER_IP
```

#### Шаг 2: Установка зависимостей
```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и Git
apt install python3 python3-pip git -y
```

#### Шаг 3: Загрузка бота
```bash
# Клонируем репозиторий
cd /home
git clone https://github.com/YOUR_USERNAME/moratalk-bot.git
cd moratalk-bot

# Устанавливаем зависимости
pip3 install -r requirements.txt
```

#### Шаг 4: Создание переменной окружения
```bash
# Создаём .env файл
nano .env
```

Добавьте:
```
BOT_TOKEN=ваш_токен_здесь
```

Сохраните (Ctrl+O, Enter, Ctrl+X)

#### Шаг 5: Создание systemd сервиса (автозапуск)
```bash
nano /etc/systemd/system/moratalk.service
```

Вставьте:
```ini
[Unit]
Description=MoraTalk Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/home/moratalk-bot
EnvironmentFile=/home/moratalk-bot/.env
ExecStart=/usr/bin/python3 /home/moratalk-bot/moratalk_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните (Ctrl+O, Enter, Ctrl+X)

#### Шаг 6: Запуск сервиса
```bash
# Перезагрузка systemd
systemctl daemon-reload

# Запуск бота
systemctl start moratalk

# Автозапуск при перезагрузке сервера
systemctl enable moratalk

# Проверка статуса
systemctl status moratalk
```

#### Шаг 7: Управление ботом
```bash
# Остановить бота
systemctl stop moratalk

# Перезапустить бота
systemctl restart moratalk

# Посмотреть логи
journalctl -u moratalk -f
```

---

## 5. Heroku (Платно)

⚠️ **Внимание**: Heroku больше не предоставляет бесплатный план (с ноября 2022).

### Стоимость: от $7/месяц

### Шаги:

1. Установите [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli)
2. В папке проекта выполните:

```bash
# Логин в Heroku
heroku login

# Создание приложения
heroku create moratalk-bot

# Добавление токена
heroku config:set BOT_TOKEN=ваш_токен

# Деплой
git push heroku main

# Масштабирование
heroku ps:scale worker=1
```

---

## 📊 Сравнение вариантов

| Платформа | Цена | Сложность | 24/7 | Рекомендация |
|-----------|------|-----------|------|--------------|
| **Render.com** | Бесплатно* | ⭐ Легко | ⚠️ С паузами | Для начала |
| **Railway.app** | $5-10/мес | ⭐ Легко | ✅ Да | Хороший баланс |
| **PythonAnywhere** | Бесплатно** | ⭐⭐ Средне | ❌ Нет | Для тестов |
| **VPS** | $2.50-5/мес | ⭐⭐⭐ Сложно | ✅ Да | Максимальный контроль |
| **Heroku** | $7/мес | ⭐⭐ Средне | ✅ Да | Надёжно |

*Бесплатный план Render усыпляет бота после 15 минут неактивности
**Бесплатный план требует ручных перезапусков

---

## 🎯 Моя рекомендация для начала:

### Вариант 1: Быстрый старт (Бесплатно)
**Render.com** - просто загрузите на GitHub и подключите к Render

### Вариант 2: Настоящий 24/7 (Дёшево)
**VPS от Vultr** ($2.50/мес) - полный контроль, работает всегда

### Вариант 3: Без заморочек (Платно)
**Railway.app** ($5-10/мес) - загрузил и забыл

---

## 🆘 Проблемы?

### Бот не запускается:
1. Проверьте токен бота
2. Убедитесь, что `BOT_TOKEN` установлен правильно
3. Проверьте логи: `journalctl -u moratalk -f` (для VPS)

### Бот останавливается:
1. Проверьте баланс на платформе
2. Убедитесь, что сервис не усыпился
3. Проверьте логи на ошибки

### Нужна помощь?
Telegram: @YOUR_SUPPORT (добавьте свой контакт)

---

## 📝 Обновление бота

### Для GitHub-based деплоя (Render, Railway):
```bash
git add .
git commit -m "Update bot"
git push origin main
```
Бот автоматически обновится!

### Для VPS:
```bash
cd /home/moratalk-bot
git pull
systemctl restart moratalk
```

---

Good luck! 🚀 Your bot will be running 24/7!
